# Product Definition

CiteWeft provides backend-neutral Rust contracts and deterministic baseline
implementations for scholarly document extraction. It preserves spans,
uncertainty, diagnostics, and provenance so downstream applications can make
their own review and canonical-data decisions.

It does not assert citation truth, replace GROBID, own provider enrichment, or
write application-specific CSL and verification state.
