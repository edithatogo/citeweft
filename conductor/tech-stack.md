# Tech Stack

- Rust 2024 edition
- Stable Rust toolchain with `rustfmt` and `clippy`
- `serde` for versioned serializable contracts
- `thiserror` for typed extraction errors
- `sha2` for deterministic routing fingerprints
- Cargo-native tests, formatting, clippy, audit, deny, and package checks
